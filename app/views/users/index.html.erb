<h1>Listing users</h1>

<table>
  <tr>
    <th>Uuid</th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= user.uuid %></td>
    <td><%= link_to 'Show', users_uuid_path(user.uuid)  %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
